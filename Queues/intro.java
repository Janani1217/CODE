/*
 * QUEUE :
 * 
 * 1. FIFO
 * 2. based on precedence we work out on queue
 * 3. functions : push(x) , pop(), front(), size()
 * 4. dynamic data structure : it can grow and also if we want to restrict the
 * size of the queue
 * 5. pop() : remove the ele which was first inserted in the que
 * 6. opp of stack
 * 
 */